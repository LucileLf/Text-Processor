<%= simple_form_for @job_request do |form| %>
  <%= form.file_field :text_file, label: 'Select a file' %>
  <%= form.input :job, collection: Job.all %>
  <%= form.input :deadline, as: :date %>
  <%= form.submit %>
<% end %>
